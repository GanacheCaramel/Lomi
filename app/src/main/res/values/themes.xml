<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème principal -->
    <style name="Theme.Lomi" parent="android:Theme.Holo.Light.DarkActionBar">
        
        
        <item name="android:statusBarColor">@color/accent_roux</item>
        <item name="android:navigationBarColor">@color/bg_nav</item>
        <item name="android:windowBackground">@color/bg_main</item>
		<item name="android:windowNoTitle">true</item>
    </style>

    <!-- Style pour les boutons -->
    <style name="ButtonCollect" parent="android:Widget.Holo.Light.Button">
        <item name="android:background">@drawable/bg_button_collect</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingLeft">18dp</item>
        <item name="android:paddingRight">18dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:radius">25dp</item>
    </style>

    <style name="ButtonCollected" parent="android:Widget.Holo.Light.Button">
        <item name="android:background">@drawable/bg_button_collect_selected</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:radius">25dp</item>
    </style>
</resources>